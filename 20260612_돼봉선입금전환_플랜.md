# 돼봉삼겹살 선입금 전환 + 직접 로그인 플랜

> 작성일: 2026-06-12
> 상태: 플랜 확정 (코드 미착수)
> 근거: 멀티에이전트 조사 (코드 + 운영 DB + git 이력 교차 확인)

---

## 0. 확정된 결정 (2026-06-12 윤지환)

| 항목 | 결정 |
|------|------|
| 발주 품목 | **비빔전용장 + 생밀면** 2종만. 양념장은 비활성 처리 |
| 돼봉 판가 (예치금 차감 기준) | **비빔전용장 132,968원**(세전 120,880) / **생밀면 64,900원**(세전 59,000) — 부가세 포함, 박스 단위. **시스템 현재값과 동일 = 단가표 변경 없음** (2026-06-12 최종 확정, 중간에 나온 107,800은 오정보) |
| 발주 주체 | **돼봉이 직접 로그인**해서 발주 (가맹점처럼) |
| 예치금 차감 시점 | **발주 등록 시** (가맹점과 동일. 부족하면 발주 자체가 안 됨 → 미수 원천 차단) |
| 최소주문금액 | **150,000원** — 가맹점과 동일 기준, 주문 총액(부가세 포함) 미만이면 발주 거절 (2026-06-12 확정) |
| 기존 후불 2건 | **수금 완료** — 예치금 잔액 0원으로 시작 |
| 아워홈·메이즈랜드 | 지금처럼 후불 유지 (이번 공사 영향 없음) |
| 단가 이원 체계 | **돼봉 청구(예치금 차감) = 돼봉 단가** / **신화수수료 = 가맹점 판가 × 육지 8.5%** (2026-06-12 사용자 확정) |

### 단가 이원 체계 예시 (생밀면 1박스 출고)
```
돼봉 예치금 차감:  64,900원 (돼봉 단가, b2b_customer_product_prices)
신화 수수료:      47,300원 × 8.5% = 4,021원 (가맹판가 기준 — 현행 코드 그대로)
```
→ 이 동작은 settlement/page.tsx:470-510에 **이미 구현돼 있음. 절대 변경 금지** (수수료 베이스를 돼봉 단가로 바꾸면 안 됨)

---

## 1. 조사로 확인된 현재 상태 (핵심만)

- **돼봉 DB 레코드**: `b2b_customers` id `ca5f8cd3-8f45-43d8-9908-0ce59b9e214f`, region=seoul, 사업자번호 비어있음, 전화 010-3074-7733('사장님')
- **신화수수료는 이미 잡힘**: 정산 5섹션이 돼봉 출고건을 "가맹점 판가 × 육지 8.5%"로 이미 합산 + 영업이익 반영 (`settlement/page.tsx:470-510`, 커밋 55c7297). → **수수료 관련 추가 작업 없음**
- **예치금 인프라는 가맹점 전용**: 잔액은 `stores.deposit_balance`, 원장은 `deposit_transactions(store_id FK)` — B2B 거래처는 못 씀 → B2B용 병렬 신설 필요
- **로그인 역할은 admin/store/shinwa 3종뿐** (`001_initial.sql:30` CHECK) — 돼봉용 `b2b` 역할 신설 필요
- **B2B 발주 API는 admin 전용** (`api/b2b/orders/route.ts:61-63`), 예치금 코드 0건
- **6/10 배포(0a4c9e1) 운영 반영 확인됨**: 원자 RPC `apply_inventory_delta`(032) 존재, 033 적용 확인, 034 CHECK 제약은 정황상 적용(아래 STEP 0에서 1줄 확정)
- **돼봉 주문 이력**: 총 2건, 모두 shipped, pending 0건 → 컷오버 시 잔여 주문 이슈 없음
- **단가 확정 경위**: 비빔전용장 돼봉 판가는 세전 120,880 / 세포함 132,968 — 마이그레이션 021의 정정이 맞았고 시스템 현재값 그대로 유지. 5/8 주문(B-20260508-004)은 옛 시드 단가(107,800)로 기록됐으나 **실제 수금은 정상 단가로 완료**(2026-06-12 사용자 확인) → 시스템 기록만 틀린 상태라 **마이그레이션 035로 소급 정정** (215,600 → 260,810, 미수 없음)

### 돼봉 정보 업데이트 내용 (STEP 1에서 반영)

| 필드 | 현재 | 변경 |
|------|------|------|
| 상호 | 돼봉삼겹살 | 돼봉삼겹살 상봉점 |
| 사업자번호 | (비어있음) | 862-31-01565 |
| 대표자 | 사장님 | 이환국 |
| 전화 | 010-3074-7733 | **010-8687-9995** (기존 번호는 메모에 보관 — 교체/병기 여부 사용자 확인) |
| 주소 | 중랑구 망우로50길24 104호 | 배송용: 서울 중랑구 상봉동 90-3 (등록증 도로명: 망우로50길 24, 104호 — 동일 건물 추정, 표기 확인) |
| 메모 | (비어있음) | 새벽배송 시 우편함 04호에 보안키 있음 |

---

## 2. 공사 설계 (왜 이렇게 하나)

**비유**: 가맹점들은 선불 교통카드(예치금)를 쓰는데, 카드 시스템이 가맹점 전용이라 돼봉은 발급 자리가 없음. 이번 공사 = **B2B용 카드 시스템을 병렬로 신설** + **돼봉에게 출입증(로그인 계정) 발급**.

**무영향 원칙 (기존 프로젝트 원칙 유지)**
- 가맹점 예치금 테이블(`deposit_transactions`, `deposit_requests`)은 손대지 않음 → B2B 전용 테이블 병렬 신설
- 출고·재고 처리는 기존 B2B 흐름 그대로 (`apply_inventory_delta` RPC 유지 — 034 CHECK 제약 때문에 재고는 반드시 이 RPC 경유)
- 기존 가맹점 코드의 알려진 약점(withdrawal 타입 CHECK 누락 버그, 비원자 차감)은 **복제하지 않고** DB 함수(RPC)로 원자 처리

---

## 3. 작업 단계 (PR 단위, 한 단계씩 진행)

### STEP 0 — 사전 점검 (코드 작업 전)
- [x] **마이그레이션 035 실행 완료** (2026-06-12) — 5/8 주문 단가 소급 정정 (215,600 → 260,810). 운영 DB 검증 완료 (합계·품목단가·로그 모두 정상)
- [ ] Supabase SQL Editor에서 034 CHECK 제약 확정 (1줄):
  `select conname from pg_constraint where conrelid='public.inventory'::regclass and conname like 'inventory_%balance';`
- [ ] 양념장 재고 14박스 불일치(시스템 29 vs 실재고 15, 5/21 미해결) — 돼봉과 무관해졌지만 별건으로 정리 권장

### STEP 1 — PR1: DB 공사 (마이그레이션 036) ✅ 완료
> 2026-06-12: `036_b2b_prepaid_deposit_and_role.sql` 운영 실행 + 검증 완료 (돼봉 is_prepaid/min_order 150,000/정보 업데이트/양념장 비활성/신규 테이블 2개/RPC — 전부 확인, 아워홈·메이즈랜드 무영향)
1. `profiles.role` CHECK에 `'b2b'` 추가 + `profiles.b2b_customer_id UUID FK` 컬럼 추가
2. `b2b_customers`에 `deposit_balance BIGINT DEFAULT 0`, `is_prepaid BOOLEAN DEFAULT false` 추가 → 돼봉만 true
3. 신규 테이블 `b2b_deposit_transactions` (b2b_customer_id FK, type: deposit/order_deduct/order_refund/adjustment, amount, balance_after, b2b_order_id FK, description, created_by)
4. 신규 테이블 `b2b_deposit_requests` (충전요청-승인 2단계, 가맹점 deposit_requests와 병렬 구조)
5. 원자 RPC `apply_b2b_deposit_delta` — 잔액 검증+차감+원장 기록을 한 트랜잭션으로
6. RLS: `b2b` 역할은 **자기 거래처 데이터만** SELECT (b2b_customers 자기 행, 자기 b2b_orders/items, 자기 단가표, 자기 예치금 원장/요청)
7. ⚠️ **원가 노출 차단**: `products`에는 cost_price, sanbang_food_sale_price_with_tax 등 민감 컬럼 존재. b2b 역할에 products 직접 SELECT 주지 말고 **안전한 view**(이름/입수/단가표 JOIN만) 또는 서버사이드 조회로 제한
8. 데이터 정리:
   - 돼봉 양념장 단가 행 `is_active=false`
   - 비빔전용장(132,968)·생밀면(64,900) 단가는 시스템 현재값 그대로 — **변경 없음**
   - 돼봉 정보 업데이트(상호/사업자번호/전화/주소/메모)

### STEP 2 — PR2: API 공사
1. `POST /api/b2b/orders`: `b2b` 역할 허용(자기 거래처 강제) + **최소주문금액 검증(min_order_amount, 돼봉 150,000 — 가맹점 api/orders:96-102 패턴 동일)** + is_prepaid면 잔액 검증 → 부족 시 400 → 충분 시 RPC로 차감+기록. admin 대행 발주도 동일 차감
2. `PATCH /api/b2b/orders/[id]`: cancel(pending/shipped) 시 환불 + 원장 기록, update 시 차액 adjustment — 가맹점 규칙과 동일
3. `DELETE`(pending): 환불 후 삭제
4. 신규 `POST/PATCH /api/b2b/deposit-requests`: b2b 역할이 충전요청 생성 → admin 승인 시 잔액 증가 + 기록
5. 출고(ship) 권한은 admin/shinwa 유지 — 돼봉은 출고 못 누름

### STEP 3 — PR3: 화면 공사
1. `src/lib/menu.ts`: `b2b` 역할 메뉴 — 발주하기(B2B), 발주내역, 예치금
2. B2B 발주 화면: b2b 역할이면 거래처 자기 자신 고정 + 잔액 표시 + 부족 시 안내 + 활성 품목 2종만
3. 예치금 화면: b2b 역할용 잔액/거래내역/충전요청 (가맹점 storeReadOnly 패턴 재사용)
4. admin 예치금관리 화면: "B2B 거래처" 섹션 — 돼봉 잔액 표시 + 수기 충전/조정
5. UI 스타일: 흑백·회색 중심 (admin/운영용 — 맥킨지 스타일)

### STEP 4 — 계정 발급 + 컷오버
- [ ] 돼봉 로그인 계정 생성 (이메일 필요 — 돼봉에 확인, 없으면 내부 발급 규칙 결정)
- [ ] profiles에 b2b 역할 + 돼봉 거래처 연결
- [ ] 돼봉에 안내: 입금 계좌, 사용법 (점주 가이드 문서의 B2B 버전 축약판)
- [ ] 입금 확인 → 충전 → 첫 선입금 발주로 검증
- [ ] 운영 검증: 발주(차감) → 취소(환불) → 재발주 → 출고 1사이클

---

## 4. 이후 결정할 소소한 것 (작업 중 확인)

1. 돼봉이 자기 pending 주문을 직접 취소/수정할 수 있게 할지 (기본안: 가능 — 가맹점과 동일)
2. 정산 화면 1섹션(가맹점 매출·예치금 차감 흐름)에 돼봉도 표시할지 (현재는 5섹션 수수료만. 기본안: 추후 필요 시)
3. 전화번호 010-3074-7733 폐기 vs 메모 보관 (기본안: 메모 보관)
4. 로그인 이메일 주소

## 5. 참고 — 이번에 안 하는 것

- 아워홈·메이즈랜드 선입금 전환 (후불 유지)
- 신화수수료 로직 변경 (이미 가맹판가 × region 요율로 정상 동작)
- 5/8 주문 단가 소급 수정 (수금 종결)
- 가맹점 예치금 withdrawal 버그 수정 (별건 — 다른 PR에서)
